{
  "type": "object",
  "required": ["id", "translations", "question_map"],
  "properties": {
    "id": "number",
    "name": "string",
    "questions": {
      "type": "array",
      "items": {
        "$ref": "./question.json"
      }
    }
  }
}